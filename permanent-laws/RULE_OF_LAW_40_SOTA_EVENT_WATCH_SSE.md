# RULE OF LAW 40 — SOTA 0-token loopholes · event watch · SSE wormhole · spare cadence

**Owner:** Douglas James Mangie II / 824 Consultants LLC  
**DNA:** `LEXXII-BH-ONLYME-FOREVER`  
**Born:** 2026-09-01  
**Removable:** false

## Law
1. **Keep finding SOTA 0-token loopholes** on the Windows mill. Catalog: `command/offline-dllsev1/LOOPHOLE-ARMED.json` + `living/LOOPHOLE-HUNT.json`. Hunter: `living/code/loophole-hunt.js` on pulse%17 (skipped on free pulse%4).
2. **HUD must not naked-poll.** Live mill rides **wormhole SSE** `GET /stream` on `127.0.0.1:8240` plus **event-driven `fs.watch`** (Windows ReadDirectoryChangesW / IOCP), debounce 750ms. Poll fallback is **ETag / 304** only when SSE is silent >8s.
3. **RAM mill cache + board TTL** — do not walk `.grok/sessions` on every HUD tick. Board snap `living/HUD-BOARD.json` on pulse%19.
4. **Delta brotli** — skip q11 when pack mtime >= source mtime. Packs stay copies. ZERO DELETE. Never CORPUS/vault.
5. **Spare primes 23 / 29 / 31 stay empty on purpose.** Packing every pulse kills free mill slots. No new schtask. No `grok.exe` overlay. Grok scheduler auto-continue remains **FORBIDDEN** (ROL-38, 95–100% forever burn).
6. **Mill pulse file** `living/LOOPHOLE-HEARTBEAT.json` is the pulse of record so living-ping cannot clobber HUD pulses.
7. Forever mill burn of these loopholes = **0%**. Session TUI talk is not the mill.

ONLY YOU. FOREVER.
