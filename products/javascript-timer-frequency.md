# JavaScript Timer Frequency vs a forever loop
**824 Consultants LLC / Lexxii · Douglas James Mangie II**  
Version 2026-09-01 · Public-safe · **Free**  
Landing: https://djmangie0824-max.github.io/824-consultants/products/javascript-timer-frequency.html

Companion to [multimedia timer resolution](multimedia-timer-resolution.html) and [When playing video](when-playing-video.html). `timeBeginPeriod` is a kernel interrupt period. When playing video is VIDEOQUALITY. This pack is the remaining Power Options row: **Internet Explorer → JavaScript Timer Frequency**. GUID `4c793e7d-a971-4716-8c81-dea2b51c3b2b`. Balanced default is often Maximum Power Savings. A JS interval in a leftover Edge/IE engine coarsens. Host-forever is Task Scheduler, not a JavaScript timer.

## What this is
`LEXXII-Materialize-Forever` is Interactive-only. The timer-resolution pack already says do not call `timeBeginPeriod(1)` as keep-awake. When sharing media can already Allow sleep. A WebView2 / Edge leftover still sits on Maximum Power Savings. JavaScript timers in that engine coarsen. Someone wired a “forever” interval inside a page. The interval naps. `schtasks` still prints Ready and last result 267009. The named Windows task is the pulse. This row is not a substitute for it. Maximum Performance on AC is the leftover honesty if the GUID exists. Missing after unhide is a skip — Windows 11 often hid Internet Explorer power settings.

Do not invent a JS `setInterval` to replace `LEXXII-Materialize-Forever`. Do not call `timeBeginPeriod(1)` to paper over this row.

Grok in-session schedulers expire in 7 days even when durable. Recreate them if missing. Host-forever on this profile is Task Scheduler `LEXXII-Materialize-Forever`. Not a Linux FULL AUTO daemon.

## When this page applies
| Symptom | Layer |
|---|---|
| ClockRes Current is 1.000 ms; a process called timeBeginPeriod | [multimedia-timer-resolution](multimedia-timer-resolution.html). Starve, not this row. |
| SYSTEM pin with no player; When sharing media is Prevent | [multimedia-sharing-sleep](multimedia-sharing-sleep.html) |
| Optimize video quality plus a looping tab | [when-playing-video](when-playing-video.html) |
| The 5m pulse was implemented as JS setInterval in the TUI | Wrong architecture. Host-forever is Task Scheduler. This row does not replace it. |
| GUID missing after unhide | This edition hid Internet Explorer power settings. Skip. Not a fail. Not a registry hack. |
| Timer resolution already honest. Sharing already Allow sleep. Named task Ready / 267009. A leftover IE/WebView engine still on Maximum Power Savings. JavaScript Timer Frequency row exists | **This page.** Maximum Performance on AC if the GUID exists. |

Do not set JavaScript Timer Frequency as the first click. Sleep, lid, and multimedia timer resolution first. Do not use this row as keep-awake. Hidden is not Maximum Performance. Missing GUID after unhide is a skip.

## What to change (plugged-in Grok desk)
1. Confirm the timer-resolution layer is already done — do not call `timeBeginPeriod(1)` as keep-awake. See [multimedia-timer-resolution](multimedia-timer-resolution.html). If ClockRes is still a leftover 1.000 ms from Chrome, that pack first.
2. Unhide the row if it is missing. Internet Explorer → JavaScript Timer Frequency is ATTRIB_HIDE on many Windows 11 images. Missing is expected. If still missing after unhide, stop.
3. Power Options → Change plan settings → Change advanced power settings → **Internet Explorer** → **JavaScript Timer Frequency**. Set **Plugged in** to **Maximum Performance**. On battery may stay Maximum Power Savings if the laptop actually travels.
4. Leave When sharing media, When playing video, and timeBeginPeriod alone. Those are siblings. This GUID is the IE/WebView timer coarsen, not WinMM.
5. Do not replace the named Windows task with a JS interval. Host-forever is `LEXXII-Materialize-Forever`.
6. Confirm.

```
powercfg -attributes 02f8986c-4424-400a-8cb3-3d804c4d70eb 4c793e7d-a971-4716-8c81-dea2b51c3b2b -ATTRIB_HIDE
powercfg /query SCHEME_CURRENT 02f8986c-4424-400a-8cb3-3d804c4d70eb 4c793e7d-a971-4716-8c81-dea2b51c3b2b
powercfg /setacvalueindex SCHEME_CURRENT 02f8986c-4424-400a-8cb3-3d804c4d70eb 4c793e7d-a971-4716-8c81-dea2b51c3b2b 1
powercfg /setactive SCHEME_CURRENT
```

Subgroup `02f8986c-4424-400a-8cb3-3d804c4d70eb` is Internet Explorer. GUID `4c793e7d-a971-4716-8c81-dea2b51c3b2b` is JavaScript Timer Frequency. Typical values: **0 = Maximum Power Savings**, **1 = Maximum Performance**. 1 on AC is the pass when the row exists. If powercfg omits the GUID after unhide, this edition did not expose the knob — stop. Do not invent a registry paste. This row does not run the 5m pulse. Host-forever is Task Scheduler. Do not paste live brokerage numbers. Do not paste `auth.json`.

## Soak test
1. Multimedia timer resolution already honest. When sharing media already Allow sleep. Otherwise this soak is lying about WinMM or a SYSTEM pin.
2. If the GUID exists: Maximum Performance on AC. A leftover WebView does not coarsen into a minute. The named task still next-runs on its own.
3. Task Scheduler still has a next-run for `LEXXII-Materialize-Forever`. Ready + 267009 with a napping JS interval is not forever. Host-forever is that named Windows task. Not systemd. Not `setInterval`.
4. Missing GUID after unhide = skip, not a red soak. Do not invent the value in the registry.
5. No fourth Grok TUI. Last result 267009 is still-running, not proof this row is already Maximum Performance. Grok in-session schedulers still expire in 7 days even when durable.

## What this page is not
- Not multimedia timer resolution. That WinMM period is [multimedia-timer-resolution](multimedia-timer-resolution.html).
- Not When sharing media. That pin is [multimedia-sharing-sleep](multimedia-sharing-sleep.html).
- Not When playing video. That quality row is [when-playing-video](when-playing-video.html).
- Not a JS keep-awake. Host-forever is Task Scheduler `LEXXII-Materialize-Forever`.
- Not a Linux systemd unit. This host is Windows.
- Not a SKU. Not live capital. NLV stays off Pages.

## Ownership
ONLY YOU. FOREVER.  
824consultants.llc@gmail.com · @DJMangie0824  
https://djmangie0824-max.github.io/824-consultants/
