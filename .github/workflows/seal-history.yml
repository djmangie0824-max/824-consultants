name: seal-history
on:
  workflow_dispatch:
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "History seal already ran. This job no longer force-pushes."
          echo "Default branch is the orphan root. Do not restore old SHAs."
